import { Paper } from '@mui/material';

export default function MemoList() {
  return (
    <Paper
      elevation={1}
      css={{
        height: '100px',
        borderRadius: 10,
        margin: 10,
      }}
    >
      메모메모
    </Paper>
  );
}
